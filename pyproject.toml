[project]
name = "organizational-network-resilience"
version = "1.1.0"
description = "L&D research package for communication-network resilience and structural knowledge-continuity risk."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
